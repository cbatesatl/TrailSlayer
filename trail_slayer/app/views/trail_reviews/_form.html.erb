<%= form_for(@trail_review, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @trail_review.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@trail_review.errors.count, "error") %> prohibited this trail_review from being saved:</h4>

      <ul>
      <% @trail_review.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <!--  <div class="form-group">
    <%= f.label :user_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :user_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :trail_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :trail_id, class: "form-control" %>
    </div>
  </div> -->
  <%= f.hidden_field :trail_id, :value => @trail.id %>
  <div class="form-group">
    <%= f.label :review, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_area :review, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
